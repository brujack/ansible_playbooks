role_dir: "{{ inventory_dir }}/playbooks/roles/common"
file_dir: "{{ role_dir }}/files"
template_dir: "{{ role_dir }}/templates"

# This setting should match the inventory group name, and the role name exactly.
# not actually used yet.
#server_group: common
